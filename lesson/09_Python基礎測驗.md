<!-- 由 python.docx 拆分；原始 Word 檔保留不變。 -->

# Python 基礎測驗卷

一、選擇題（每題 4 分，共 20 分）

1. Python 是什麼？  
A. 一種電腦遊戲  
B. 一種程式語言  
C. 一種作業系統  
D. 一種圖片格式

2. 下列哪一個是 Python 的輸出指令？  
A. input()  
B. print()  
C. type()  
D. len()

3. 下列哪一個變量名稱是正確的？  
A. 1score  
B. my value  
C. student_name  
D. class

4. input() 的回傳資料型態通常是什麼？  
A. int  
B. float  
C. str  
D. bool

5. 下列哪一個是比較運算子？  
A. =  
B. +  
C. ==  
D. print

二、是非題（每題 2 分，共 10 分）

1. Python 程式對縮排不敏感。  
（ ）

2. == 是用來比較兩個值是否相等。  
（ ）

3. 列表（list）中的第一個元素索引是 0。  
（ ）

4. int("5") 可以把字串 5 轉成整數 5。  
（ ）

5. while 迴圈一定只會執行一次。  
（ ）

三、填充題（每題 4 分，共 20 分）

## 1. print() 是用來________。

## 2. Python 程式檔案的副檔名是________。

## 3. 變量是用來_______資料的。

## 4. input() 預設回傳的資料型態是________。

## 5. if 判斷中，如果條件成立，會執行_______的程式。

四、程序題（共 50 分）

題目 1：輸入兩個數字，計算總和（20 分）

請寫一個程式，讓使用者輸入兩個數字，然後輸出它們的總和。

要求：

要使用 input()

要把輸入轉成整數

要使用 print() 顯示結果

題目 2：成績判斷（15 分）

請寫一個程式，讓使用者輸入一個成績，判斷是否及格。

要求：

成績大於或等於 60 顯示「及格」

否則顯示「不及格」

題目 3：猜數字遊戲（15 分）

請寫一個簡單的猜數字遊戲。

要求：

答案固定為 7

使用者輸入一個數字

如果猜中，顯示「答對了」

如果沒有猜中，顯示「再試一次」

參考答案

一、選擇題

B

B

C

C

C

二、是非題

錯

對

對

對

錯

三、填充題

顯示內容

.py

儲存

str

成立時

四、程序題參考答案

題目 1

```python
a = int(input("請輸入第一個數字："))
b = int(input("請輸入第二個數字："))

print(a + b)
```

題目 2

```python
score = int(input("請輸入成績："))

if score >= 60:
    print("及格")
else:
    print("不及格")
```

題目 3

```python
answer = 7
guess = int(input("請猜一個數字："))

if guess == answer:
    print("答對了")
else:
    print("再試一次")
```
